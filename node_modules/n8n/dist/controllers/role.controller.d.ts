import { CreateRoleDto, UpdateRoleDto } from '@n8n/api-types';
import { Role as RoleDTO } from '@n8n/permissions';
import { RoleService } from '../services/role.service';
export declare class RoleController {
    private readonly roleService;
    constructor(roleService: RoleService);
    getAllRoles(): Promise<Record<string, RoleDTO[]>>;
    getRoleBySlug(slug: string): Promise<RoleDTO>;
    updateRole(slug: string, body: UpdateRoleDto): Promise<RoleDTO>;
    deleteRole(slug: string): Promise<RoleDTO>;
    createRole(body: CreateRoleDto): Promise<RoleDTO>;
}
